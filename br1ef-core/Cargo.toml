[package]
name = "br1ef-core"
version = "0.1.0"
edition = "2024"

[dependencies]
imap = "2"
native-tls = "0.2"
mailparse = "0.15"
chrono = { version = "0.4", default-features = false, features = ["clock"] }
anyhow = "1"
